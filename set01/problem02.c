//2. Write a C program to add two numbers.
#include<stdio.h>
int main() {
    int x = 4;
    int y = 6;
    int sum = 0;
    sum = x + y;
    printf("the sum of %d and %d is %d\n", x, y, sum);
    return 0;
}